# Performs strsplit and keeps first n elements of each string

Performs strsplit and keeps first n elements of each string

## Usage

``` r
strsplit_keep_last(x, split, as_numeric = F)
```

## Arguments

- x:

  vector

- split:

  separator

- as_numeric:

  try to convert to numeric
