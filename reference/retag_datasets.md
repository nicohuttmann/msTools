# Change name or copy data before saving the RData environment

Change name or copy data before saving the RData environment

## Usage

``` r
retag_datasets(tag = "_x", copy.files = FALSE)
```

## Arguments

- tag:

  suffix for data names

- copy.files:

  should data frames with other names be copied (default = F)
