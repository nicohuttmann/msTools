# Transform values of x (row numbers) into groups of n as characters with equal number of leading zero's

Transform values of x (row numbers) into groups of n as characters with
equal number of leading zero's

## Usage

``` r
partition_n2char(x, n)
```

## Arguments

- x:

  vector to work on

- n:

  number of partitions
