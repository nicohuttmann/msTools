# Transforms matrix to tibble and adds row names as first column

Transforms matrix to tibble and adds row names as first column

## Usage

``` r
m2t(data, to.row.names = "rows")
```

## Arguments

- data:

  matrix with row names

- to.row.names:

  name for row names column
