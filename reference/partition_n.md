# Transform values of x (row numbers) into groups of n

Transform values of x (row numbers) into groups of n

## Usage

``` r
partition_n(x, n)
```

## Arguments

- x:

  vector to work on

- n:

  number of partitions
