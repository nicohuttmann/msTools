# Registers the default column sets used by the DIA-NN importers

Registers the default column sets used by the DIA-NN importers

## Usage

``` r
.add_defaults()
```

## Value

nothing; `Info$defaults` is created in the global environment
