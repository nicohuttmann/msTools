# Vector with elements as names

Vector with elements as names

## Usage

``` r
cc(...)
```

## Arguments

- ...:

  vector/s whose elements become their own names

## Value

a character vector named by its own elements
