# Transforms id numbers into cahracters with equal number of leading zero's

Transforms id numbers into cahracters with equal number of leading
zero's

## Usage

``` r
num2idchar(x, as.numeric = F)
```

## Arguments

- x:

  vector to work on

- as.numeric:

  return numeric ids instead of characters
