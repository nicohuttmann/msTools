# Align and expand rows of a list of tibbles

Chooses the first column if nothing is supplied to argument by.

## Usage

``` r
alignexp_tibble_rows(x, by = NULL)
```

## Arguments

- x:

  list of tibbles

- by:

  column to join and align
