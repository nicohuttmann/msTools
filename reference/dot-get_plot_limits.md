# Extract the built x/y axis ranges of a ggplot

Extract the built x/y axis ranges of a ggplot

## Usage

``` r
.get_plot_limits(plot)
```

## Arguments

- plot:

  a ggplot object

## Value

list with `xmin`, `xmax`, `ymin`, `ymax`
